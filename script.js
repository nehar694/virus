// Optional: JavaScript to change the text inside the bubble dynamically
window.addEventListener('load', function() {
  const inputText = document.getElementById('inputText');

  // Example of setting a value programmatically, replace with your desired logic
  setTimeout(() => {
    inputText.textContent = "Dynamic Input";
  }, 2000); // After 2 seconds, text will change to "Dynamic Input"
});
